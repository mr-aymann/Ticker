const createMovieTable = require('./movieSchema');
const createTheaterTable = require('./theatreSchema');


// Initialize all tables
const initSchemas = async () => {
    // await createTheaterTable();
    // await createMovieTable();
};

module.exports = initSchemas;
